# Redirects from what the browser requests to what we serve
/images/:folder/full/:file    /.netlify/images?url=/images/:folder/:file
/images/:folder/:width/:file   /.netlify/images?url=/images/:folder/:file&width=:width